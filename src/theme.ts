import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    background: {
      default: '#efefef',
    },
    primary: {
      main: '#26a248',
    },
    secondary: {
      main: '#0e4d1f',
    },
    info: {
      main: '#f2f2bb'
    }
  },
});
